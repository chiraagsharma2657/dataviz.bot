# Get a key at https://aistudio.google.com/apikey
GOOGLE_API_KEY="your-api-key-here"

# Show the landing page and make every visitor supply their own key, even when
# GOOGLE_API_KEY is set above. Turn this on to preview the landing page, and
# when hosting publicly so visitors spend their own quota instead of yours.
# REQUIRE_USER_KEY=true

# Optional: override the default Gemini model
# MODEL_NAME="gemini-3.6-flash"
